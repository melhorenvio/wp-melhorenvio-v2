me-box-sizing-reset()
    if selector() is "&"
        html
            box-sizing: border-box

        *, *:before, *:after
            box-sizing: inherit
    else
        *, *:before, *:after
            box-sizing: border-box

box-sizing-reset = me-box-sizing-reset unless me-no-conflict
